# 试卷批改系统 — 规格说明（SPEC）

> 本文件是需求与设计的唯一权威。改功能前先改这里，再改代码。

---

## 1. 这个系统是干什么的

给**一位老师**用的单机版电子试卷批改工具。老师把学生答卷（扫描件或照片）导进来，
在屏幕上逐题打分、用红笔在卷面上圈画写评语，系统自动算总分、出班级分析、导成绩表，
批注过的卷子可以导出发回给学生。

> ### ⚠️ 只批主观题
> **本系统只批改主观题**（解答、简答、计算、作文、论述）。
> **选择题、判断题、填空题这类客观题不进系统** —— 老师已经在系统外改好了。
> 想在成绩表里算总分，就在批改台顶部填一个「客观题得分」合计分；不想算就把
> 「客观题满分」留成 0，整块界面自动隐藏，系统只统计主观题。
> 因此：**不做客观题自动判分、题型里没有单选多选判断填空。**
>
> ### ⚠️ 判分标准：意思相近即可得分
> 主观题**有参考答案**（`question.answer_key`），但判分**不要求和参考答案字面一致**：
> 换个说法、调了语序、用同义词、写得更简略但意思对，都要正常给分。
> 只有要点确实缺失、说错、答非所问才扣分。
> 这条既写进 AI 的提示词，也写在批改台的界面上提醒老师。

**不做**：学生端答题、多老师协作、局域网/云端同步、账号体系。就是一台机器一个人用。

---

## 2. 运行环境（最关键的约束）

老师的机器是 **华为擎云 L540-031**。**2026-07-29 已在那台机器上实际确认**：

```
$ cat /etc/os-release      →  PRETTY_NAME="UOS Desktop 20 Pro"
                              ID=uos  VERSION_ID="20"  VERSION_CODENAME=eagle
$ git --version            →  git version 2.20.1
```

- **系统：统信 UOS Desktop 20 Pro（代号 eagle，基于 Debian 10 "buster"）**
- **git 已装（2.20.1）** → 交付和更新都可以走 `git clone` / `git pull`，zip 是备用路线
- **Python：Debian 10 自带 3.7.3**（未逐字确认，但 UOS 20 的基座决定了就是这个）
- **CPU 架构：仍未确认**（`uname -m` 老师连试三次都打错）。**但不重要** ——
  零依赖设计下 x86_64 和 aarch64 跑起来完全一样

| 约束 | 后果 |
|---|---|
| 国产 Linux，不是 Windows | 交付脚本是 `.sh` + `.desktop`，不是 `.bat`；`.sh` 必须是 LF 换行 |
| 可能是 ARM64 | **绝对不能依赖任何需要编译的 pip 包**（numpy / Pillow / PyMuPDF / opencv 在 ARM 上没有现成轮子，会现场编译，编译失败远程救不回来） |
| Python 3.7.3 | 语法基线锁 **Python 3.7**，不用 3.8+ 的新语法和新 API |
| 自带浏览器较老 | 前端 JS 基线锁 **ES2017**，CSS 用保守写法，不引 CDN |
| 政企机器可能有网络管控 | 除 AI 批改外，**全部功能离线可用**；AI 是可关闭的开关 |

**基线不是靠自觉守的**：`tests/test_baseline.py` 会用 `ast.parse(feature_version=(3,7))`
真按 3.7 的规则解析一遍全部产品代码，并扫掉 3.8+/3.9+ 才有的标准库 API、
ES2018+ 的 JS 写法、以及页面里的外链。开发机是 3.12，**能跑不代表那边能跑**。

**因此：本项目零第三方依赖。只用 Python 标准库 + 浏览器原生能力。**
不写 `requirements.txt`（写了就是给人装东西的信号），不用 pip。

开发机是 Windows（`D:\项目\试卷批改系统`），所以代码必须跨平台：路径走 `pathlib`，
文件读写一律显式 `encoding="utf-8"`。

---

## 3. 技术方案

| 层 | 选型 | 为什么不用常规方案 |
|---|---|---|
| 后端 | 标准库 `http.server.ThreadingHTTPServer` + `sqlite3` | Flask/FastAPI 要 pip |
| 前端 | 原生 HTML + CSS + JS，无框架无构建 | 老师机器上不可能跑 npm |
| 卷面显示 | `<img>` 显示 PNG/JPG | 见 §3.1 |
| 红笔批注 | HTML5 Canvas 覆盖在图片上，前端合成导出 | 服务端合成要 Pillow |
| 导出表格 | 手写 `.xlsx`（zipfile + 手拼 XML）+ UTF-8 BOM 的 CSV 兜底 | openpyxl 要 pip |
| 导出 PDF | 浏览器打印（CSS `@page`），老师点「打印 → 另存为 PDF」 | reportlab 要 pip |
| 调大模型 | 标准库 `urllib.request` | requests 要 pip |
| 测试 | 标准库 `unittest` | pytest 要 pip（虽然只在开发机跑，但保持一致） |

### 3.1 PDF 怎么处理（这是本项目最取巧的一步）

扫描仪扫出来的 PDF，**每一页本质上就是一张 JPEG 或 PNG 被套进了 PDF 容器**。
所以不需要渲染引擎，只要**把内嵌的图片流原样抠出来**就行：

- 用标准库手写一个极简 PDF 解析器（`server/pdfimg.py`），扫描所有 `/Subtype /Image` 对象
- `/DCTDecode` → 流数据本身就是完整 JPEG 文件，直接写成 `.jpg`
- `/FlateDecode` → `zlib.decompress` 得到原始像素，用 `zlib` + `struct` 手工封装成 `.png`
- `/JPXDecode`(JPEG2000)、`/CCITTFaxDecode`(传真压缩) → 当前不支持，明确报错并提示老师换格式

抠不出来的 PDF（born-digital 的矢量/文字型 PDF，不是扫描件）会给出明确提示：
「这个 PDF 不是扫描件，请用手机拍照或另存为图片后再导入」。**不静默失败。**

对应地，前端永远只跟普通图片打交道 —— 标注坐标换算精确，也不用打包 pdf.js。

---

## 4. 数据模型（SQLite）

```
exam        考试     id, name, subject, klass, exam_date, full_score, pass_score,
                    excellent_score, objective_full, created_at, updated_at
question    题目     id, exam_id, sort_order, no_label, qtype, max_score,
                    stem, answer_key, rubric, knowledge_point
student     学生     id, exam_id, sort_order, name, student_no, note
paper       答卷     id, exam_id, student_id, status, total_score, objective_score,
                    comment, graded_at, updated_at
page        答卷页面 id, paper_id, page_no, image_path, width, height, annotated_path
score       得分     id, paper_id, question_id, score, student_answer,
                    ai_suggested_score, ai_comment, ai_accepted, comment, updated_at
annotation  批注     page_id, data_json, updated_at     -- Canvas 笔画数据，可重编辑
```

AI 接口配置（含 API key）不进数据库，单独存 `data/config.json`，方便老师手改、也确保不进 git。

要点：
- `question.qtype` ∈ `essay`(解答) `short`(简答) `calc`(计算) `composition`(作文)
  `discuss`(论述) `other`(其他) —— **全是主观题，没有客观题类型**
- 题型只影响统计口径和 AI 提示词的措辞，判分方式都一样：老师给分
- `question.stem` 题干、`answer_key` 参考答案、`rubric` 评分要点 —— 三者一起喂给 AI，
  批改台上也直接显示给老师对照（判分标准是「意思相近即可」，不是字面比对）
- `score.score` 是**唯一权威分数**。AI 的建议分存在 `ai_suggested_score`，
  只有老师点了确认（`ai_accepted=1`）才会被写进 `score.score`
- `paper.objective_score` 是客观题合计分，由老师手填（`exam.objective_full` 为 0 时整块隐藏）
- `paper.total_score` = 各主观题得分之和 + `objective_score`，是缓存值，任何一次改分都重算
- 所有数据在 `data/` 下，**`data/` 永不进 git，更新脚本永不覆盖**
- 新版本要加字段就往 `db.ADDED_COLUMNS` 里加，`init()` 会在老师的旧库上自动 `ALTER TABLE`。
  **只许加列，不许改列型、不许删列** —— 老师那边的库里是真实成绩

---

## 5. 功能清单

### 5.1 考试管理
建/改/删考试；填考试名、科目、班级、日期、满分、及格分、优秀分。删考试要二次确认且级联删数据。

### 5.2 题卡设置
**只登记要批的主观题。** 逐题定义：题号（如「三、1」「四」）、题型、满分、题干、评分要点、知识点。
- 支持批量加题（「加 6 道解答题，每题 12 分」一次生成）
- 支持从上一场考试复制题卡
- 「客观题满分」在考试设置里填一个数（选择判断那部分的总分），填 0 表示这次不统计客观题

### 5.3 学生名单
粘贴名单（一行一个，支持「学号 姓名」或纯姓名）或导入 CSV。可手动增删改。

### 5.4 答卷导入
三种来源都支持：
1. 一个学生一张或多张照片（JPG/PNG）
2. 一个学生一个 PDF
3. **全班一个大 PDF** → 按页拆成图片 → 进入「绑定页面到学生」界面（可设「每人 N 页」自动切分，也可手动调整）

导入的原图存 `data/uploads/<exam_id>/`，数据库只存相对路径。

### 5.4b 标准答案卷
老师同样可以上传**标准答案**的扫描件/照片/PDF（走同一条导入管线），整场考试共用一份，
存在 `answer_page` 表，文件放 `data/uploads/<exam_id>/_key/`。
用途有两个：批改台上一键对照查看；AI 批改时连同学生答卷一起发给模型。
文字版的参考答案则填在题卡的 `answer_key` 字段里，两者可以同时用。

### 5.5 批改台（核心）
```
┌───────────────────────────┬──────────────────┐
│                           │ 张三  第 3/45 人 │
│      卷面图片             │ 客观题 [ 42 ]/45 │
│   （缩放 / 拖动 / 翻页）  │ ─────────────────│
│                           │ 三、1 解答 12分  │
│   上面盖一层 Canvas       │  [ 9 ] /12  🤖AI │
│   画红勾 ✓ 叉 ✗ 圈 字     │ 三、2 解答 12分  │
│                           │  [__] /12   🤖AI │
│                           │ 四  作文  60分   │
│  工具条：勾 叉 画笔 文字  │ ─────────────────│
│         橡皮 撤销 清空    │ 总分 78 / 150    │
│                           │ [上一个][下一个] │
└───────────────────────────┴──────────────────┘
```
- 每道主观题直接输分；也可以点 AI 出建议分 + 评语草稿，老师可采纳、改数、或忽略
- 顶部「客观题得分」一个输入框（`exam.objective_full` 为 0 时不显示）
- 快捷键：`数字键` 输分、`Enter` 下一题、`←/→` 上/下一个学生、`Ctrl+Z` 撤销笔画、
  `1-5` 切批注工具、`空格` 本题给满分、`0` 本题给 0 分
- 批注笔画存成矢量 JSON（可回来接着改），导出时前端合成 PNG
- 每次改分即时落库，不需要「保存」按钮；断电最多丢当前一笔

### 5.6 成绩与分析
- 成绩总表：学号、姓名、各题得分、总分、排名
- 班级分析：平均分、最高/最低、中位数、及格率、优秀率、分数段分布直方图
- 题目分析：每题得分率、满分人数、零分人数，按得分率升序排「最该讲评的题」
- 知识点分析：按 `knowledge_point` 汇总得分率
- 个人报告：单个学生的总分、排名、错题清单、老师评语，可直接打印

### 5.7 导出
- 成绩总表 `.xlsx`（自己拼的，WPS / LibreOffice 都能开）
- 成绩总表 `.csv`（UTF-8 带 BOM，防止 WPS 打开乱码）
- 错题清单 `.xlsx`
- 带批注的答卷图片（单个学生打包下载）
- 各类报告走浏览器打印 → 另存为 PDF

### 5.8 AI 辅助批改（智谱 GLM-5V）

**默认模型：智谱 `glm-5v-turbo`**（2026-07-29 用户确认的实际型号）。选它是因为它
**能看图片** —— 可以直接对着扫描的答卷批改，老师不用把学生写的字一个个打进电脑。

**已用真接口验证**（2026-07-29）：2 页答卷 + 1 页标准答案的整卷批改，耗时约 23 秒，
准确认出卷面手写字；学生措辞与参考答案不同仍给满分，说明「意思相近即得分」的提示词生效。

⚠️ `glm-5v-turbo` 是**思考型**模型：返回里除了 `content` 还有 `reasoning_content`，
光推理就要烧一百多个 token。**绝对不许给它设 `max_tokens`** —— 设小了推理会把额度吃光，
`content` 直接是空字符串（探测时踩过）。也因为要思考，默认超时给到 180 秒。
`/models` 接口只列文本模型，不列视觉模型，**不能拿它判断视觉模型存不存在**。

- 设置页填 `base_url` / `api_key` / `model` / **「这个模型能看图」开关**，
  走 **OpenAI 兼容的 `/chat/completions`**。换通义千问 VL / DeepSeek 只改配置不改代码，
  设置页提供一键填入预设（见 `config.AI_PRESETS`）
- ⚠️ **纯文本模型（DeepSeek `deepseek-chat` 等）看不了卷子图**。选了这类模型，
  界面必须提示老师「要把学生作答文字粘进输入框才能批」，不能假装能看图
- 两种批法：
  1. **整卷预批**（`/api/ai/grade_paper`）：把【标准答案卷图】+【学生答卷图】一次发过去，
     模型一次返回所有主观题的建议分和评语，写进 `ai_suggested_score` / `ai_comment`
  2. **单题批**（`/api/ai/suggest`）：只批一道题，可带图，也可只批老师粘贴的文字
- 送给模型的内容：题干、**参考答案**、评分要点、满分，加上答卷图片和/或学生作答文字。
  提示词里明确写死判分原则：**意思相近即得分，不要求和参考答案字面一致**
- 图片**由前端用 Canvas 压缩成 dataURL 再发给后端**，后端只转发 ——
  这样不用在老师机器上装任何图像库（ARM 上装不了）
- 模型返回：建议分 + 评语 + 逐要点比对。**永远只是草稿，老师不点「采纳」就不落库**
- 卷面看不清时要求模型返回 `score: null` 并说明原因，**不许瞎猜分数**
- `api_key` 存在 `data/API_KEY.txt`（老师只需往里粘一行，读到就自动启用）或
  `data/config.json`，**两者都在 .gitignore 里，绝不进代码仓**
- 网络不通/未配置时，AI 按钮灰掉并给出原因，其余功能完全不受影响

---

## 6. 交付与更新

| 文件 | 用途 |
|---|---|
| `install.sh` | 老师机器上跑一次：检查 Python、创建 `data/`、生成桌面图标 |
| `start.sh` | 启动服务并自动打开浏览器 |
| `update.sh` | 更新代码：有 `git` 就 `git pull`；没有就下载 zip 覆盖。**`data/` 一个字节都不碰** |
| `试卷批改系统.desktop` | Linux 桌面图标模板 |
| `启动.bat` | 仅供 Windows 开发机自测用 |

- 远程仓：GitHub 公开仓。代码不含任何隐私，学生数据只在老师本地
- 页面右上角常驻显示 `VERSION` 文件里的版本号 —— 老师点完更新，念一下版本号就知道生效没有
- 更新脚本更新前自动把当前代码备份到 `data/_backup_<版本号>/`，出问题能滚回去

---

## 7. 明确的非目标

- **不批客观题**：选择、判断、填空不进系统，不做标准答案、不做自动判分（2026-07-28 用户明确要求）
- 不做 OMR 答题卡自动识别（涉及图像处理，ARM 上没有依赖可用）
- 不做手写体 OCR（同上）
- 不做学生端、不做多老师、不做联网同步
- v1 不做 AI 看图批改（见 §5.8）
- 不做用户登录（单机单人，加了只是给老师添麻烦）
