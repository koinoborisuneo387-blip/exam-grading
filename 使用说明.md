# 试卷批改系统 · 使用说明

> 这份是给老师看的。遇到打不开、报错、看不懂的地方，把屏幕拍张照片发给开发者就行。

---

## 一、每天怎么用（三步）

1. 双击桌面上的 **「试卷批改系统」** 图标。
2. 会先跳出一个黑色窗口（这是程序在跑，**不要关**），然后浏览器自动打开界面。
3. 用完了，把那个黑色窗口关掉就是停止。

> 数据都存在你自己这台电脑上，不联网也能批卷子、算成绩、导表格。
> 只有「AI 帮着批」这一项需要联网。

---

## 二、一场考试的完整流程

### 第 1 步：建考试
首页填考试名称、班级、科目、满分，点「建立」。

- **客观题满分**：选择题判断题你已经在系统外改好了。
  想让成绩表里出总分，就把那部分的满分填进去（比如 45）；
  不想统计就填 **0**，界面上跟客观题有关的东西会全部隐藏。

### 第 2 步：登记题目（题卡）
**只登记你要批的主观题**（解答、简答、计算、作文），选择题判断题不用管。

每道题填：题号、题型、满分。再往下展开可以填：

| 填什么 | 有什么用 |
|---|---|
| 题干 | 让 AI 知道这题在问什么 |
| **参考答案** | AI 照着它判分。**判分标准是「意思相近就给分」**，不要求学生写得和它一模一样 |
| 评分要点 | 比如「修辞 4 分、效果 4 分、情感 4 分」，AI 会按点给分 |
| 知识点 | 填了以后，成绩分析里会告诉你哪个知识点全班最弱 |

题多的话用 **「批量加题」**：填「加 6 道、每题 12 分、题号前缀 三、」，一次生成。
下次考试还能用 **「从别的考试复制题卡」**。

### 第 3 步：上传标准答案（可选，但建议传）
在「考试设置 / 题卡」页最下面，把参考答案的**扫描件或照片**传上去。
批改时可以随时点「标准答案」对照看；用 AI 批的时候也会一起发过去，判得更准。

### 第 4 步：导入学生和答卷
点顶上的「学生与答卷」。

**名单不用先建。** 卷子上本来就写着姓名，直接传答卷，AI 会读出来（见下面）。

如果你手上已经有名单，也可以粘进去，一行一个：
```
01 张三
02 李四
03 王五
```
只写姓名也可以，从 Excel 里复制两列粘过来也认。
**下次考试直接点「从上次考试复制」** —— 班级名单一学期不变，这样最省事。

**传答卷**，两种方式随便挑：

- **一个一个传**：在名单里点某个学生后面的「传答卷」，选他的照片或 PDF。
- **整班一起传**：扫描仪扫出来一个大 PDF，直接拖到下面的方框里。
  系统会拆成一页一页，然后按下面的办法分给学生。

#### 让 AI 认名字（推荐）

传完之后，点 **「🤖 让 AI 读卷面上的姓名」**。它会读出每份卷子表头的姓名和学号，
自动判断哪几页是同一个人的，然后列一张确认表给你：

| 颜色 | 意思 | 你要做什么 |
|---|---|---|
| 🟢 绿 | 和名单里的人对上了 | 扫一眼就行 |
| 🟡 黄 | 会新建这个学生，或者拿不准是谁 | 看一下对不对 |
| 🔴 红 | 没读出姓名 | **必须你来指定**，不然不让导入 |

姓名读错了，**直接在框里改**；想绑到别的学生，用右边的下拉框选。
都核对完点「确认导入」，名单和答卷一起就建好了。

> ⚠️ **AI 读出来的名字只是草稿，一定要过一眼。**
> 名字认错了，分数就记到别的同学头上去了，而且要等整班批完才会发现。
> 所以系统故意不做「全自动」，必须你点确认才算数。
> 名字只有两个字的时候（比如「张三」和「张山」），系统**不会**帮你猜，会让你自己定。

**不想用 AI，或者 AI 没配**：点「取消，改用手动分配」，回到手动模式 ——
点选几页 → 选学生 → 绑定；每人页数一样的话，填「每人 N 页」点「按顺序自动分配」最快。

> **关于 PDF**：必须是**扫描件**（扫描仪扫的、手机拍的）。
> 电脑里直接生成的文字版 PDF 读不了，系统会明确告诉你，让你改用拍照。
> 如果扫描仪扫出来提示「黑白传真压缩格式读不了」，
> 到扫描仪设置里把颜色改成**彩色**或**灰度**，重扫一次就好。

### 第 5 步：批改
点顶上的「批改」。左边是卷子，右边是打分。

> 「批改」打开后会**自动接到上次没批完的那个学生**——昨天批到第 20 个，
> 今天打开直接就是第 20 个，不用自己翻。想批某个特定的人，
> 去「学生与答卷」或「成绩分析」里点他后面的「批改」。

**打分**
- 直接在每题的框里输分数，或者点「满分」「0 分」。
- <kbd>回车</kbd> 跳到下一题，<kbd>空格</kbd> 给满分，<kbd>←</kbd> <kbd>→</kbd> 换学生。
- **不用点保存**，输完就自动存了。

**在卷子上画红笔**
- 上面工具条选 ✏画笔 / ✓勾 / ✗叉 / ◯圈 / A文字 / ⌫擦。
- 画错了点「撤销」，或者 <kbd>Ctrl</kbd>+<kbd>Z</kbd>。
- 画完点 **「存批注图」**，之后可以在成绩页把带红笔的卷子打包下载发给学生。

**让 AI 先批一遍**（要先配好 API Key，见第四节）
- 点 **「🤖 AI 预批整卷」**：把标准答案和这个学生的卷子一起发给 AI，
  一次就把所有主观题批完，给出建议分和评语。
- 也可以在某一道题上点「让 AI 批这道题」。
- ⚠️ **AI 给的分不会直接算成成绩**。你看一眼觉得合适，点「采纳这个分」才算数。
  不同意就自己输一个，或者干脆不理它。

> 想一次把全班都让 AI 先批一遍：到「学生与答卷」页，点 **「AI 预批全班」**。
> 45 个人大概几分钟，中途可以停。批完你再一个个过一遍点采纳。

### 第 6 步：看成绩、导表格
点顶上的「成绩分析」，能看到：

- 平均分、最高最低、及格率、优秀率、分数段分布
- **最该讲评的题**（按得分率从低到高排）
- 哪个知识点全班最弱
- 成绩总表（含名次）

导出：
- 「下载成绩表（Excel）」—— WPS 直接能开，里面有三个表：成绩总表、班级分析、题目分析
- 「下载错题清单」
- 「下载全班批注卷」—— 全班带红笔的卷子打成一个包，一人一个文件夹，
  解压后可以整包发到班级群。图片多的话要等一小会儿
- 「打印 / 存成 PDF」—— 在打印窗口里把打印机选成「另存为 PDF」
- 每个学生后面的「报告」—— **个人成绩单**：总分、名次、每题得分和班级平均的对比、
  哪些题该补强、你的评语，一页纸，点「打印」就能打出来或存成 PDF 发给家长
- 每个学生后面的「下载批注卷」—— 只下载这一个学生的

---

## 三、更新程序

开发者改好了新功能，你双击桌面上的 **「更新试卷批改系统」** 就行。

- 更新**只换程序**，你的成绩、答卷、批注、API Key **一个都不会丢**。
- 更新完，关掉正在运行的程序窗口，重新双击「试卷批改系统」。
- 打开网页看**右上角的版本号**，变了就说明更新成功了。

---

## 四、配置 AI（只用做一次）

1. 到 **https://open.bigmodel.cn** 注册智谱账号，实名，在控制台里创建一个 API Key，复制。
2. 找到程序目录下的 **`data` 文件夹**，里面有个 **`API_KEY.txt`**。
3. 用记事本（或文本编辑器）打开它，把 Key 粘到最下面空的那一行，**保存**。
   带 `#` 号的几行是说明，不用动，也不用删。
4. 回到网页，点右上角 **「AI 设置」**，看到绿色的「✓ 已从这个文件读到密钥」就成了。
5. 点一下「测试连接」，显示「连接正常」就可以用了。

**关于花钱**：按用量计费，批一道题几厘到几分钱。

**如果测试连接失败**：多半是这台电脑上不了外网（单位网络管控）。
那就把 AI 关掉，其它功能照常用，人工打分一点不受影响。

---

## 五、备份数据（强烈建议）

所有成绩、答卷、批注都**只存在你这台电脑上**，电脑一旦坏了就全没了。
所以**每批完一场考试，花一分钟备份一次**：

1. 点右上角「AI 设置」，拉到「数据备份」那一块。
2. 点 **「下载备份文件」**，等浏览器提示下载完成。
3. 把下载好的压缩文件复制到 **U 盘**（或传到网盘）保存。

万一哪天要恢复：把备份压缩包解压，用里面的 `data` 文件夹替换程序目录下的
`data` 文件夹，重新启动程序，成绩就都回来了。

---

## 六、常见问题

**只想批一两个人的卷子（补考、漏交的），怎么弄最快？**
不用走整班导入那一套：到「学生与答卷」页，
① 他不在名单里就在名单框里粘一行加进去；
② 点他那一行的「传答卷」，选他的照片或 PDF —— 会直接归到他名下，不用再分配；
③ 点同一行的「批改」就能批了。批完成绩和排名自动更新。

**双击图标没反应？**
在图标上点鼠标右键 → 「允许运行」或「信任此文件」，再双击一次。

**浏览器没自动打开？**
自己打开浏览器，地址栏输入 `127.0.0.1:8899` 回车。

**黑窗口一闪就没了？**
说明程序起不来。在程序目录上点右键选「在终端中打开」，输入 `bash start.sh` 回车，
把出现的报错拍照发给开发者。

**PDF 传上去说读不了？**
它多半不是扫描件。用手机把卷子拍成照片导入最省事。

**改错分了？**
直接把框里的数字改掉就行，或者点「清空」回到未批状态。改了总分和排名会自动跟着变。

**能撤销删除吗？**
删考试、删学生是**删了就没了**，会问你两遍。答卷图片、成绩、批注会一起删掉，请想清楚。

**数据在哪里？**
程序目录下的 `data` 文件夹。备份最省事的办法见上面第五节 ——
「AI 设置」页里点「下载备份文件」；手动把整个 `data` 文件夹复制到 U 盘也行，效果一样。
