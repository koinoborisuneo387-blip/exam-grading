# 试卷批改系统 — AI 协作规则

> 本文件与同目录 `AGENTS.md` 内容必须**逐字一致**：改了一个，必须同步改另一个。
> 任何 AI 助手（Claude Code / Codex / 其他）动这个项目之前，先读完本文件，再读 `SPEC.md`。

---

## 0. 第一铁律：零第三方依赖

**这个项目一个 pip 包都不许装。只用 Python 标准库。**

用户的老师用的是 **华为擎云 L540-031 + 银河麒麟 V10 / 统信 UOS V20，大概率 ARM64**。
在那台机器上 `pip install numpy / Pillow / PyMuPDF / opencv / Flask / requests / openpyxl / reportlab`
会因为没有 ARM 预编译轮子而现场编译，编译到一半失败，开发者远程根本救不回来。

所以：

- **不要创建 `requirements.txt`。** 存在这个文件本身就是"要装东西"的信号。
- 想用某个库之前先问：标准库能不能干？答案基本都是能，只是要多写点代码。已有的替代实现：
  - 图片渲染 PDF → `server/pdfimg.py`（手写 PDF 解析，抠内嵌图片流）
  - Excel 导出 → `server/xlsx.py`（zipfile + 手拼 XML）
  - HTTP 客户端 → `urllib.request`
  - PNG 编码 → `zlib` + `struct`
  - Web 框架 → `http.server.ThreadingHTTPServer`
  - 图像合成 → 扔给前端 Canvas 做
- 前端同理：**不装 npm 包、不引 CDN、无构建步骤**。所有 JS/CSS 都是手写的静态文件。
  引 CDN 会在离线或网络管控的机器上直接白屏。

## 1. 第二铁律：语法基线

老师那台机器 2026-07-29 已确认是 **统信 UOS Desktop 20 Pro**（代号 eagle，
基于 Debian 10），自带 **Python 3.7.3**，**git 2.20.1 已装**。

| | 基线 | 禁用 |
|---|---|---|
| Python | **3.7** | 海象 `:=`(3.8)、`f"{x=}"`(3.8)、`dict\|dict`(3.9)、内建泛型 `list[str]`(3.9)、`str.removeprefix`(3.9)、`match`(3.10)、`functools.cache`(3.9)、`zoneinfo`(3.9)、`copytree(dirs_exist_ok=)`(3.8) |
| JavaScript | **ES2017** | 可选链 `?.`、空值合并 `??`、对象展开 `{...x}`(ES2018)、`Array.flat`、`replaceAll`、顶层 `await`、私有字段 `#x`、`structuredClone` |
| CSS | 保守 | `:has()`、容器查询、`aspect-ratio`、嵌套语法 |

开发机是 Python 3.12，**语法能跑不代表老师机器能跑**。
类型标注要用就写 `from __future__ import annotations` + `typing.List` 这类旧写法。

**这条不用靠自觉**：`tests/test_baseline.py` 会用 `ast.parse(feature_version=(3,7))`
真按 3.7 的规则解析全部产品代码，并扫掉新版标准库 API、新 JS 语法、页面外链、
以及任何第三方 import。**跑一次 `python -m unittest discover -s tests -t tests` 就知道有没有越线。**

## 2. 数据安全铁律

- **`data/` 目录是老师的真实数据（成绩、答卷原图、批注）。** 任何脚本都不许删它、覆盖它、
  移动它。`update.sh` 只动代码文件。
- **`data/config.json` 里有 API key，永远不进 git。** 已在 `.gitignore` 中。
  不要把 key、密码、绝对路径写进任何被 git 跟踪的文件。
- 删考试这类破坏性操作必须二次确认，且要在后端再校验一次，不能只靠前端弹窗。

## 3. 产品铁律

- **只批主观题。** 选择题、判断题、填空题**不进这个系统**（2026-07-28 用户明确要求）。
  不要"顺手"加回标准答案、自动判分、单选多选题型 —— 老师那部分另外处理，
  系统里只有一个「客观题得分」合计输入框。曾经写过一版自动判分逻辑，按这条要求删掉了。
- **AI 给的分永远只是草稿。** `score.score` 只能由老师的明确操作写入。
  不许加"AI 自动落分""一键全部采纳 AI 建议"这类口子 —— 这是给学生打分，错了要老师负责。
- **AI 从卷面读出的姓名同样只是草稿。** 必须走「确认姓名」界面，老师点头才建名单、才绑答卷。
  不许加"信任 AI，直接建名单"的开关。理由：认错名字 = 分数记到别人头上，
  而且要等整班批完才会发现，比批错分难收拾得多。姓名只有 2 个字时**不做模糊匹配**
  （"张三"和"张山"编辑距离也是 1）。读不出姓名的页必须标红逼老师处理，不许静默跳过。
- **抠不出图的 PDF 要明确报错**，告诉老师"这不是扫描件，请拍照或另存为图片"。
  不许静默失败或给一张空白页 —— 老师会以为是自己操作错了。
- 面向老师的文案一律用大白话，不出现「实例」「渲染」「序列化」这类词。
  报错信息要写"接下来该干什么"，不是只甩一个异常名。

## 4. 本地开发

| 项 | 值 |
|---|---|
| 开发目录 | `D:\项目\试卷批改系统`（独立 git repo，分支 `main`） |
| 本地地址 | http://127.0.0.1:8899 |
| 启动 | `python app.py` 或双击 `启动.bat` |
| 测试 | `python -m unittest discover -s tests -v` |
| 版本号 | `VERSION` 文件，页面右上角角标读 `/api/version` |

- **改完 Python 必须重启服务进程**，`app.py` 不热加载。改 `static/` 下的文件刷新浏览器即可。
- 改了代码就抬 `VERSION`（纯文档改动不抬）—— 老师那边全靠版本号确认更新到底生效没有。
- 提交前跑一遍 `python -m unittest discover -s tests`。

## 5. 工作方式

- **先出方案，等用户确认了再写代码。** 讨论需求时不要边聊边改：先说清做法、影响面、
  可选项和推荐，停下来等确认。用户已经说「开工 / 就这么做」的除外。
- 改功能前**先改 `SPEC.md`**，再改代码。SPEC 是需求的唯一权威。
- 需要用户亲手做的事（部署、填 API key、在老师机器上操作），**攒成一批**一次性交给他，
  不要一件一件打断他。API key 一律由用户自己填，AI 不碰。
